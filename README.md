best way to run it is just clone the repo, npm install, npm run dev frontend and backend

stack - next.js frontend, node.js backend, supabase DB hosting

utilized spotify webAPI + LLaMa7B for playlist generation
